# LumaPost · GitHub Secrets 完整配置指南

> 一份配置，让邮件自动发送 + AI智能摘要

---

## 📖 目录

1. [什么是 Secrets](#1-什么是-secrets)
2. [完整配置清单](#2-完整配置清单)
3. [如何添加 Secrets](#3-如何添加-secrets)
4. [各配置项详解](#4-各配置项详解)
5. [不同场景的配置示例](#5-不同场景的配置示例)
6. [常见问题](#6-常见问题)

---

## 1. 什么是 Secrets

GitHub Secrets 是存储在 GitHub 仓库中的**加密环境变量**，用于存放敏感信息（如密码、API密钥）。它们：

- 🔒 **加密存储**：只有 GitHub Actions 运行时才能访问
- 👻 **不可见**：任何人（包括仓库管理员）都无法查看已设置的 Secret 的原始值
- 🚫 **不提交**：永远不会被提交到代码仓库中

> ⚠️ **重要**：Secrets 一旦设置，就无法查看原始值。如需修改，只能重新设置覆盖。

---

## 2. 完整配置清单

### 📧 SMTP 邮件配置（必须）

| Secret 名称 | 是否必须 | 说明 | 示例值 |
| :--- | :--- | :--- | :--- |
| `SMTP_HOST` | ✅ 必须 | SMTP 邮件服务器地址 | `smtp.gmail.com` |
| `SMTP_PORT` | ✅ 必须 | SMTP 服务器端口 | `465` |
| `SMTP_SECURE` | ✅ 必须 | 是否使用 SSL/TLS 加密 | `true` |
| `SMTP_USER` | ✅ 必须 | 发件邮箱账号 | `your-email@gmail.com` |
| `SMTP_PASS` | ✅ 必须 | SMTP 密码/授权码/应用密码 | `abcd efgh ijkl mnop` |
| `MAIL_TO` | ✅ 必须 | 收件人邮箱地址 | `receiver@example.com` |

### 🧠 AI 模型配置（可选，如需要 AI 摘要）

| Secret 名称 | 是否必须 | 说明 | 示例值 |
| :--- | :--- | :--- | :--- |
| `AI_PROVIDER` | ⚪ 使用AI时必须 | 指定 AI 服务商 | `deepseek` |
| `AI_API_KEY` | ⚪ 使用AI时必须 | AI 服务商的 API 密钥 | `sk-xxxxx` |
| `AI_MODEL` | ❌ 可选 | 指定使用的模型名称 | `deepseek-v4-flash` |
| `AI_BASE_URL` | ❌ 可选 | 自定义 API 端点 | `https://api.deepseek.com` |
| `AI_TIMEOUT` | ❌ 可选 | 请求超时时间（毫秒） | `60000` |
| `AI_MAX_TOKENS` | ❌ 可选 | 最大输出 token 数 | `4096` |
| `AI_TEMP` | ❌ 可选 | 回复创造性 (0-1) | `0.7` |

### 🔧 辅助配置（可选）

| Secret 名称 | 是否必须 | 说明 | 示例值 |
| :--- | :--- | :--- | :--- |
| `CITY` | ❌ 可选 | 天气查询城市（默认北京） | `Shanghai` |
| `LANG` | ❌ 可选 | 输出语言 | `zh-CN` |

---

## 3. 如何添加 Secrets

### 步骤一：进入 Secrets 设置页面
1.进入你的 GitHub 仓库

2.点击顶部的 Settings 标签

3.在左侧菜单栏点击 Secrets and variables

4.选择 Actions

### 步骤二：添加新的 Secret
1.点击右上角的 New repository secret 按钮

2.在 Name 输入框中填入 Secret 名称（如 SMTP_HOST）

3.在 Secret 输入框中填入对应的值

4.点击 Add secret 保存


### 步骤三：重复添加所有需要的 Secrets

按照上面的清单，逐个添加所有必须的 Secret。

> 💡 **提示**：添加时名称必须**完全一致**（区分大小写），否则 GitHub Actions 无法读取。

---

## 4. 各配置项详解

### 📧 SMTP 邮件配置

这些配置决定了邮件如何发送，**所有项都必须填写**。

#### 不同邮箱服务商的配置参考

| 邮箱服务商 | `SMTP_HOST` | `SMTP_PORT` | `SMTP_SECURE` | `SMTP_PASS` 说明 |
| :--- | :--- | :--- | :--- | :--- |
| **Gmail** | `smtp.gmail.com` | `465` | `true` | 使用[应用专用密码](https://myaccount.google.com/apppasswords) |
| **QQ邮箱** | `smtp.qq.com` | `465` | `true` | 使用[授权码](https://service.mail.qq.com/detail/0/75) |
| **163邮箱** | `smtp.163.com` | `465` | `true` | 使用[授权码](https://help.mail.163.com/faqDetail.do?code=d7a5dc8471cd0c0e8b4b8f4f8e49998b374173cfe9171305fa1ce630d7f67ac2) |
| **Outlook/Hotmail** | `smtp-mail.outlook.com` | `587` | `false` | 使用邮箱密码 |
| **腾讯企业邮** | `smtp.exmail.qq.com` | `465` | `true` | 使用邮箱密码 |
| **阿里企业邮** | `smtp.mxhichina.com` | `465` | `true` | 使用邮箱密码 |

#### ⚠️ 重要说明

- **`SMTP_PASS`**：Gmail、QQ、163 等邮箱**不是**登录密码，需要去邮箱设置中单独生成授权码/应用密码
- **`SMTP_PORT` + `SMTP_SECURE`** 组合：
  - 端口 `465` + `SMTP_SECURE=true`：使用 SSL 加密
  - 端口 `587` + `SMTP_SECURE=false`：使用 STARTTLS（需要额外配置 `SMTP_TLS=true`，本例未支持）

---

### 🧠 AI 模型配置

如果不配置 AI 相关 Secret，项目会**跳过 AI 摘要步骤**，依然可以正常发送邮件（只是没有 AI 生成的内容）。

#### 各 AI 服务商的 `AI_PROVIDER` 与 `AI_API_KEY` 获取

| `AI_PROVIDER` | 服务商 | API Key 获取方式 | 官网 |
| :--- | :--- | :--- | :--- |
| `deepseek` | DeepSeek | [API Keys 页面](https://platform.deepseek.com/api_keys) | https://platform.deepseek.com |
| `minimax` | MiniMax | [接口密钥页面](https://platform.minimaxi.com/user-center/basic-information/interface-key) | https://platform.minimaxi.com |
| `openai` | OpenAI | [API Keys 页面](https://platform.openai.com/api-keys) | https://platform.openai.com |
| `anthropic` | Claude | [API Keys 页面](https://console.anthropic.com/settings/keys) | https://console.anthropic.com |
| `google` | Google Gemini | [API Keys 页面](https://aistudio.google.com/app/apikey) | https://aistudio.google.com |
| `qwen` | 阿里通义千问 | [API-KEY 管理](https://dashscope.console.aliyun.com/apiKey) | https://dashscope.aliyun.com |
| `zhipu` | 智谱 GLM | [API Keys 页面](https://open.bigmodel.cn/usercenter/apikeys) | https://open.bigmodel.cn |
| `doubao` | 字节豆包 | [API Key 管理](https://console.volcengine.com/ark/region:ark+cn-beijing/apiKey) | https://console.volcengine.com/ark/ |
| `kimi` / `moonshot` | Kimi | [API Keys 页面](https://platform.moonshot.cn/console/api-keys) | https://platform.moonshot.cn |
| `stepfun` | 阶跃星辰 | 在开发者平台创建 | https://platform.stepfun.com |
| `groq` | Groq | [API Keys 页面](https://console.groq.com/keys) | https://console.groq.com |
| `openrouter` | OpenRouter | [API Keys 页面](https://openrouter.ai/keys) | https://openrouter.ai |

#### 可用模型参考（`AI_MODEL`）

| `AI_PROVIDER` | 可用模型（默认值**加粗**） |
| :--- | :--- |
| `deepseek` | `deepseek-v4-flash`**,** `deepseek-v4-pro` |
| `minimax` | `MiniMax-M3`**,** `MiniMax-M2`, `MiniMax-Text-01` |
| `openai` | `gpt-4.1`, `gpt-4o`, **`gpt-4o-mini`**, `gpt-4-turbo` |
| `anthropic` | **`claude-3-7-sonnet-20250219`**, `claude-3-5-sonnet-20241022` |
| `google` | `gemini-2.5-pro`, **`gemini-2.5-flash`**, `gemini-2.0-flash` |
| `qwen` | `qwen3.7-max`, **`qwen3.7-plus`**, `qwen3.6-flash` |
| `zhipu` | **`glm-4.5-plus`**, `glm-4.5-air`, `glm-4.5-flash` |
| `doubao` | **`doubao-pro-256k`**, `doubao-pro-128k`, `doubao-lite-256k` |
| `kimi`/`moonshot` | `moonshot-v1-8k`, `moonshot-v1-32k`, **`moonshot-v1-128k`** |
| `groq` | **`llama-3.3-70b-specdec`**, `llama-3.1-70b`, `mixtral-8x7b` |

> 💡 如果不设置 `AI_MODEL`，会使用**加粗**的默认模型。

---

## 5. 不同场景的配置示例

### 场景一：最小化配置（只发邮件，不用AI）

只需配置 6 个 SMTP Secret：

| Secret 名称 | 值（示例） |
| :--- | :--- |
| `SMTP_HOST` | `smtp.gmail.com` |
| `SMTP_PORT` | `465` |
| `SMTP_SECURE` | `true` |
| `SMTP_USER` | `my@gmail.com` |
| `SMTP_PASS` | `abcd efgh ijkl mnop` |
| `MAIL_TO` | `my@example.com` |

### 场景二：DeepSeek + 邮件推送（国内推荐）

| Secret 名称 | 值（示例） |
| :--- | :--- |
| `SMTP_HOST` | `smtp.qq.com` |
| `SMTP_PORT` | `465` |
| `SMTP_SECURE` | `true` |
| `SMTP_USER` | `123456@qq.com` |
| `SMTP_PASS` | `授权码` |
| `MAIL_TO` | `my@example.com` |
| `AI_PROVIDER` | `deepseek` |
| `AI_API_KEY` | `sk-xxxxx` |
| `AI_MODEL` | `deepseek-v4-flash` |

### 场景三：完整配置（所有可选参数）

| Secret 名称 | 值（示例） |
| :--- | :--- |
| `SMTP_HOST` | `smtp.gmail.com` |
| `SMTP_PORT` | `465` |
| `SMTP_SECURE` | `true` |
| `SMTP_USER` | `my@gmail.com` |
| `SMTP_PASS` | `应用专用密码` |
| `MAIL_TO` | `my@example.com` |
| `AI_PROVIDER` | `qwen` |
| `AI_API_KEY` | `sk-xxxxx` |
| `AI_MODEL` | `qwen3.7-plus` |
| `AI_TIMEOUT` | `120000` |
| `AI_MAX_TOKENS` | `8192` |
| `AI_TEMP` | `0.8` |
| `CITY` | `Shanghai` |

---

## 6. 常见问题

### Q1: 如何确认 Secrets 是否配置正确？

**方法一：查看 Actions 日志**
1. 运行一个 Workflow
2. 点击进入运行详情
3. 查看日志中是否有明显的报错信息（如 `Missing environment variable`）

**方法二：在 Workflow 中调试**
在 `email.yml` 中添加以下步骤（注意：**调试完成后必须移除**）：
```yaml
- name: Debug Secrets
  run: |
    echo "SMTP_HOST: ${SMTP_HOST}"
    echo "AI_PROVIDER: ${AI_PROVIDER}"
    # 不要打印 SMTP_PASS 等敏感值！

    Q2: 我修改了 Secrets，需要重新 push 代码吗？
不需要。Secrets 是独立存储的，修改后会自动生效，下次运行 Workflow 时就会使用新值。

Q3: 如何删除一个 Secret？
进入 Secrets 设置页面，找到要删除的 Secret，点击右侧的 删除按钮（垃圾桶图标），确认即可。

Q4: Secret 的值可以包含特殊字符吗？
可以。但要确保：

不包含换行符

如果包含空格或引号，建议用引号包裹（如 "my secret value"）

Q5: 多个仓库需要同样的 Secret，需要重新设置吗？
需要。GitHub Secrets 是仓库级别的，每个仓库都需要单独设置。

Q6: 为什么我设置了 Secret 但 Actions 中读取不到？
可能的原因：

名称不一致：检查 Secret 名称是否与代码中 process.env.XXX 完全一致（区分大小写）

作用域错误：确认是在 Settings > Secrets and variables > Actions 中添加的，不是 Environments 或 Dependabot

未重新运行：添加 Secret 后，需要重新运行 Workflow 才能生效

Q7: 配置了 AI 但没有 AI 摘要内容？
检查 AI_PROVIDER 和 AI_API_KEY 是否正确

确认 AI 账户是否有可用额度

查看 Actions 日志中是否有 AI 调用报错

注意：AI 摘要可能因为内容太长而被截断或跳过

📋 配置检查清单
在开始使用前，请逐项确认：

基础配置（全部必须 ✅）
□ SMTP_HOST 已配置
□ SMTP_PORT 已配置
□ SMTP_SECURE 已配置
□ SMTP_USER 已配置（发件邮箱）
□ SMTP_PASS 已配置（密码/授权码）
□ MAIL_TO 已配置（收件邮箱）
AI 配置（按需启用）
□ AI_PROVIDER 已配置
□ AI_API_KEY 已配置
□ AI_MODEL 已配置（可选）
□ 已确认 AI 账户有足够额度
测试验证
□ 在 Actions 中手动触发一次工作流
□ 检查邮箱是否收到测试邮件
□ 检查邮件内容是否完整（天气、新闻、AI摘要等）
📞 获取帮助
如果配置过程中遇到问题：

查看 GitHub Actions 的运行日志（红色部分是错误信息）

确认邮箱的 SMTP 授权码/应用密码已正确生成

确认 AI API Key 有效且账户有余额

在项目 Issues 中提交问题：https://github.com/Mao2973464622/lumapost/issues

配置完成！现在你的 LumaPost 就可以每天自动发送邮件了 🎉

